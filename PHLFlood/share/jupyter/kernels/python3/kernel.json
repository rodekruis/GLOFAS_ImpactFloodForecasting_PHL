{
 "note": "This repository should not contain environment-specific Jupyter kernel specs. Actual kernel.json files are created and managed by user or CI environments (e.g., via conda or pip-installed ipykernel) and should be excluded from version control."
}
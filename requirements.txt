# PhilFlood - Impact-Based Flood Forecasting System
# Core dependencies for EVT calibration and CLIMADA integration
# Install with: pip install -r requirements.txt

# These 4 packages are CRITICAL and should not be modified
# All other dependencies (numpy, pandas, xarray, netCDF4, etc.) are automatically installed by these

climada>=3.0.0          # Flood hazard modeling, centroids, impact calculation
climada-petals>=1.0.0   # Regridding and flood depth interpolation
ipywidgets>=7.6.0       # Interactive Jupyter notebook diagnostics
pyextremes>=2.3.0       # Extreme Value Theory: POT/GPD calibration
